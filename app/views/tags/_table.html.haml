%table.table.table-compressed.table-hover.table-striped
  %thead
    %tr
      %th Title
      %th Times Used
  %tbody.js-clickable-rows
    - tags.each do |tag|
      %tr{data: {href: "#{tag_path(tag)}"}}
        %td= tag.title
        %td= tag.resources.count
