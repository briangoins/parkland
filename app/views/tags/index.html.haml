.page-header
  %h1
    All Tags
    .pull-right
      - if can? :create, Tag
        = link_to new_tag_path, class: "btn btn-lg btn-success" do
          %i.fa.fa-plus
          Add

= render 'table', tags: @tags